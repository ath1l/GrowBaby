<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Weekly Nutrition Summary - Grow Baby</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/animejs/3.2.1/anime.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap');

        :root {
            --hex-current-1: #7360DF;
            --hex-current-2: #C499F3;
            --hex-current-3: rgba(196, 153, 243, 0.1);
            --text-dark: #33186B;
            --text-light: #7360DF;
            --pink-accent: #F2AFEF;
            --pink-accent-light: rgba(242, 175, 239, 0.15);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #F2AFEF 0%, #C499F3 25%, #7360DF 50%, #33186B 75%, #F2AFEF 100%);
            min-height: 100vh;
            color: var(--text-dark);
            position: relative;
            overflow-x: hidden;
        }

        /* Animated background elements */
        body::before {
            content: '';
            position: fixed;
            top: -10%;
            left: -10%;
            width: 120%;
            height: 120%;
            background:
                radial-gradient(circle at 20% 80%, rgba(242, 175, 239, 0.3) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(196, 153, 243, 0.3) 0%, transparent 50%),
                radial-gradient(circle at 40% 40%, rgba(115, 96, 223, 0.3) 0%, transparent 50%);
            animation: float 25s ease-in-out infinite;
            z-index: -1;
        }

        body::after {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background:
                url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%237360DF' fill-opacity='0.1'%3E%3Ccircle cx='30' cy='30' r='2'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
            z-index: -1;
            animation: dots 30s linear infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0) rotate(0deg) scale(1); }
            33% { transform: translateY(-20px) rotate(120deg) scale(1.05); }
            66% { transform: translateY(-10px) rotate(240deg) scale(0.95); }
        }

        @keyframes dots {
            0% { transform: translateX(0) translateY(0); }
            100% { transform: translateX(60px) translateY(60px); }
        }

        /* Header styles */
        .main-header {
            background-color: #c89af000;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: relative;
            z-index: 10;
        }

        .header-container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 28px;
            font-weight: 700;
            color: #ff6b9d;
            text-decoration: none;
        }

        .main-nav a {
            margin: 0 1rem;
            text-decoration: none;
            color: #555;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .main-nav a:hover {
            color: var(--hex-current-1);
        }

        .my-baby {
            display: flex;
            align-items: center;
            text-decoration: none;
            color: #555;
            font-weight: 500;
        }

        .my-baby svg {
            width: 24px;
            height: 24px;
            margin-right: 8px;
            fill: #555;
        }

        .gradient-bar {
            height: 5px;
            background: linear-gradient(to right, #F2AFEF, #C499F3, #7360DF);
        }

        .container {
            max-width: 900px;
            margin: 2rem auto;
            padding: 30px 20px;
            position: relative;
            z-index: 1;
        }

        .title {
            text-align: center;
            margin-bottom: 40px;
        }

        .title h1 {
            color: #ffffff00;
            font-size: 48px;
            font-weight: 800;
            margin-bottom: 8px;
            letter-spacing: -1px;
            position: relative;
        }

        .split-char {
            color: #6f55ff;
            background-color: rgb(196 153 243 / 0%);
            outline: none;
            border-radius: .25rem;
            display: inline-block;
            padding: 0.1rem 0.2rem;
            margin: 0.05rem;
        }

        .title p {
            color: var(--hex-current-1);
            font-size: 16px;
            font-weight: 500;
            text-shadow: 0 2px 10px rgba(115, 96, 223, 0.2);
        }

        .card {
            background: linear-gradient(145deg, rgb(255 255 255) 0%, rgb(255 255 255 / 23%) 100%);
            backdrop-filter: blur(25px);
            padding: 32px;
            border-radius: 28px;
            box-shadow:
                0 25px 50px rgba(242, 175, 239, 0.15),
                0 15px 35px rgba(196, 153, 243, 0.1),
                inset 0 1px 0 rgba(255, 255, 255, 0.8);
            margin-bottom: 28px;
            border: 2px solid rgba(242, 175, 239, 0.3);
            position: relative;
            overflow: hidden;
        }

        .card h3 {
             margin-top: 32px;
             margin-bottom: 16px;
             font-weight: 600;
             color: var(--text-light);
             font-size: 20px;
             border-bottom: 2px solid var(--hex-current-3);
             padding-bottom: 8px;
        }
        
        /* Table Styles */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 1em;
            background: var(--hex-current-3);
            border-radius: 12px;
            overflow: hidden;
        }
        th, td {
            padding: 14px 18px;
            text-align: center;
            border-bottom: 1px solid rgba(196, 153, 243, 0.3);
        }
        th {
            background-color: rgba(115, 96, 223, 0.1);
            color: var(--text-light);
            font-weight: 600;
        }
        td {
            color: #5c538d;
            font-weight: 500;
        }
        tr:last-child th, tr:last-child td {
            border-bottom: none;
        }

        /* AI Summary Box */
        .ai-summary {
            background: var(--pink-accent-light);
            padding: 1.2em;
            border-left: 5px solid var(--pink-accent);
            border-radius: 8px;
            margin-bottom: 2em;
            color: #8c5b8a;
            line-height: 1.7;
        }
        .ai-summary p {
            margin: 0;
            white-space: pre-line;
        }

        .no-logs {
            text-align: center;
            font-weight: 500;
            color: var(--text-light);
            padding: 2rem;
            background-color: var(--hex-current-3);
            border-radius: 12px;
        }

        .chart-container {
            margin-top: 2.5em;
        }

    </style>
</head>

<body>
    <!-- Consistent Header from Homepage -->
    <header class="main-header">
        <div class="header-container">
            <a href="/" class="logo">Baby Joy</a>
            <nav class="main-nav">
                <a href="/">Home</a>
                <a href="/milestone">Milestone</a>
                <a href="/resources">Resources</a>
                <a href="/chatbot">Chatbot</a>
                <a href="/nutrition">Nutrition</a>
            </nav>
            <a href="/profile" class="my-baby">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z" />
                </svg>
                <span>
                    <% if (typeof babyName !== 'undefined') { %>
                        <%= babyName %>
                    <% } else { %>
                        My Baby
                    <% } %>
                </span>
            </a>
        </div>
    </header>
    <div class="gradient-bar"></div>

    <div class="container">
        <div class="title">
            <h1 id="page-title">Weekly Nutrition Summary</h1>
            <p>A visual overview of your baby's diet from the last 7 days.</p>
        </div>

        <div class="card">
            <% if (typeof aiSummary !== 'undefined' && aiSummary && aiSummary.trim().length > 0) { %>
                <div class="ai-summary">
                    <p><%= aiSummary %></p>
                </div>
            <% } %>

            <% if (typeof summary === 'undefined' || summary.length === 0) { %>
                <p class="no-logs">No food logs available for the last 7 days to generate a summary.</p>
            <% } else { %>
                <h3>Daily Totals</h3>
                <table>
                    <thead>
                        <tr>
                            <th>Date</th>
                            <th>Calories</th>
                            <th>Protein (g)</th>
                            <th>Carbs (g)</th>
                            <th>Fats (g)</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% summary.forEach(day => { %>
                            <tr>
                                <td><%= day.date %></td>
                                <td><%= day.calories.toFixed(2) %></td>
                                <td><%= day.protein.toFixed(2) %></td>
                                <td><%= day.carbs.toFixed(2) %></td>
                                <td><%= day.fats.toFixed(2) %></td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>

                <div class="chart-container">
                    <canvas id="nutritionChart"></canvas>
                </div>
            <% } %>
        </div>
    </div>

    <script>
        // Animate the page title
        const textElement = document.getElementById('page-title');
        if (textElement) {
            const text = textElement.textContent;
            textElement.innerHTML = text.split('').map(char => {
                if (char === ' ') { return ' '; }
                return `<span class="split-char">${char}</span>`;
            }).join('');

            anime({
                targets: '.split-char',
                translateY: [
                    { value: -10, duration: 500 },
                    { value: 0, duration: 500 }
                ],
                loop: true,
                delay: anime.stagger(100),
                direction: 'alternate',
                easing: 'easeInOutQuad'
            });
        }

        // Render Chart if data is available
        <% if (typeof chartData !== 'undefined' && chartData) { %>
            const ctx = document.getElementById('nutritionChart').getContext('2d');
            const chartData = <%- JSON.stringify(chartData) %>;

            // Set Chart.js global defaults
            Chart.defaults.font.family = 'Poppins, sans-serif';
            Chart.defaults.color = '#5c538d';

            new Chart(ctx, {
                type: 'line',
                data: {
                    labels: chartData.labels,
                    datasets: [
                        { label: 'Calories', data: chartData.calories, borderColor: '#ff6b9d', backgroundColor: '#ff6b9d20', tension: 0.3, fill: true },
                        { label: 'Protein (g)', data: chartData.protein, borderColor: '#7360DF', backgroundColor: '#7360DF20', tension: 0.3, fill: true },
                        { label: 'Carbs (g)', data: chartData.carbs, borderColor: '#C499F3', backgroundColor: '#C499F320', tension: 0.3, fill: true },
                        { label: 'Fats (g)', data: chartData.fats, borderColor: '#f2afef', backgroundColor: '#f2afef20', tension: 0.3, fill: true }
                    ]
                },
                options: {
                    responsive: true,
                    plugins: {
                        legend: { 
                            position: 'top',
                            labels: {
                                font: {
                                    weight: '500'
                                }
                            }
                        },
                        title: { 
                            display: true, 
                            text: 'Daily Nutrition Breakdown (Last 7 Days)',
                            font: {
                                size: 16,
                                weight: '600'
                            },
                            padding: {
                                top: 10,
                                bottom: 20
                            }
                        }
                    },
                    scales: {
                        y: {
                            beginAtZero: true,
                            grid: {
                                color: 'rgba(196, 153, 243, 0.2)'
                            },
                            ticks: {
                                color: '#7360DF'
                            }
                        },
                        x: {
                            grid: {
                                display: false
                            },
                             ticks: {
                                color: '#7360DF'
                            }
                        }
                    }
                }
            });
        <% } %>
    </script>
</body>

</html>
